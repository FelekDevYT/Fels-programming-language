using "fels.lang.sfm"

class Buffer {
    buffer = ""

    func init(b) {
        buffer = b
    }

    func set(b) {
        buffer = b
    }

    func get() {
        return buffer
    }

    func append(b) {
        buffer += b
    }
}