using "fels.lang.sfm"

func isPrime(n) {
    if(n <= 1) return false
    for(i = 2, i * i <= n, i++) {
        if(n % i == 0) return false
    }
    return true
}

println(isPrime(29)) // true
println(isPrime(15)) // false