using "fels.lang.sfm"

class StringBuilder {
    val = NULL

    func StringBuilder() {
    }

    func append(value) {
        val = val + value
    }

    func set(value) {
        val = value
    }

    func get() {
        return val
    }
}
