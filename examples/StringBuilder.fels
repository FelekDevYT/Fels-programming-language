using "fels.lang.sfm"

class StringBuilder {
    val = NULL

    func init() {
    }

    func append(value) {
        val = val + value
    }

    func set(value) {
        val = value
    }

    func get() {
        return val
    }
}
